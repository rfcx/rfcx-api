apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api-mqtt-ingress
  namespace: production
  labels:
    app: "api-mqtt"
spec:
  rules:
    - host: mqtt.rfcx.org
      http:
        paths:
          - backend:
              serviceName: api-mqtt-service
              servicePort: 80
