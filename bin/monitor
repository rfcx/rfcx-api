#!/usr/bin/env bash

export SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )";

export TIMEOUT=1800

$SCRIPT_DIR/monitor-expect-tail-logs 1 $TIMEOUT &
$SCRIPT_DIR/monitor-expect-tail-logs 2 $TIMEOUT &
